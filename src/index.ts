// Main exports for the FSS Parse Word TypeScript library
export { WordParser } from './word-parser';
export { SafetyManager } from './safety-manager';
export * from './types';

// Default export
export { WordParser as default } from './word-parser';